git & shell
git reset HEAD^ 撤回上次提交
git reset HEAD 撤销暂存区的内容
git stash
git pull
git commit -m 'feat: xxx’ --no-verify
git history
git log

切换版本：volta pin node@18.19.0
终止端口：kill -9 $(lsof -t -i:3030)


npm是发布这4个文体：bin, dist, readme, package
npm login
npm publish
npm version patch
npm config get registry